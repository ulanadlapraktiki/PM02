<UserControl x:Class="TechnologistModule.Views.DeviationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="⚠️ Отклонения и события" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnRefresh" Content="🔄 Обновить" Background="#7F8C8D" Foreground="White" Padding="15,8" Height="40"/>
            <Button x:Name="btnResolve" Content="✅ Закрыть" Margin="10,0,0,0" Background="#27AE60" Foreground="White" Padding="15,8" IsEnabled="False" Height="40"/>
            <Button x:Name="btnDelete" Content="🗑️ Удалить" Margin="10,0,0,0" Background="#E74C3C" Foreground="White" Padding="15,8" IsEnabled="False" Height="40"/>
            <ProgressBar x:Name="progress" Width="100" Height="4" Margin="10,0,0,0" IsIndeterminate="True" Visibility="Collapsed"/>
        </StackPanel>

        <DataGrid x:Name="grid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" 
                  Margin="0,10,0,0" FontSize="13" AlternatingRowBackground="#F8F9FA"
                  SelectionChanged="Grid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID партии" Binding="{Binding BatchId}" Width="80"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding DeviationType}" Width="120"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="250"/>
                <DataGridTextColumn Header="Важность" Binding="{Binding Severity}" Width="80"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding ReportedAt, StringFormat=dd.MM.yyyy HH:mm}" Width="120"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding ResolvedAt, Converter={StaticResource ResolvedConverter}}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>