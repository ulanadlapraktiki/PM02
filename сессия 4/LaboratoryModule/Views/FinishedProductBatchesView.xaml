<UserControl x:Class="LaboratoryModule.Views.FinishedProductBatchesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🏷️ Готовая продукция - контроль качества" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnRefresh" Content="🔄 Обновить" Background="#7F8C8D" Foreground="White" Padding="15,8"/>
            <ProgressBar x:Name="progress" Width="100" Height="4" Margin="10,0,0,0" IsIndeterminate="True" Visibility="Collapsed"/>
        </StackPanel>

        <DataGrid x:Name="grid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" 
          Margin="0,10,0,0" FontSize="13" AlternatingRowBackground="#F8F9FA"
          SelectionChanged="Grid_SelectionChanged"
          MouseDoubleClick="Grid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер партии" Binding="{Binding BatchNumber}" Width="130"/>
                <DataGridTextColumn Header="Кол-во, кг" Binding="{Binding ActualQuantityKg}" Width="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="130"/>
                <DataGridTemplateColumn Header="Действие" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="🔬 Анализ" Background="#27AE60" Foreground="White" 
                            Width="70" Height="25" FontSize="11" Cursor="Hand"
                            Click="BtnAnalyze_Click" Tag="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>