<UserControl x:Class="OperatorModule.Views.ActiveBatchesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и кнопка в одной строке -->
        <Grid Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="🏭 Активные партии" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button x:Name="btnRefresh" Content="🔄 Обновить" Background="#3498DB" 
                    Foreground="White" Width="120" Height="35" 
                    Grid.Column="1" Cursor="Hand"/>
        </Grid>

        <!-- Таблица -->
        <DataGrid x:Name="grid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                  FontSize="14" AlternatingRowBackground="#F8F9FA"
                  MouseDoubleClick="Grid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№ партии" Binding="{Binding BatchNumber}" Width="120" FontWeight="Bold"/>
                <DataGridTextColumn Header="Продукт" Binding="{Binding ProductName}" Width="200"/>
                <DataGridTextColumn Header="Линия" Binding="{Binding LineNumber}" Width="80"/>
                <DataGridTextColumn Header="Текущий шаг" Binding="{Binding CurrentStep}" Width="200"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding BatchStatus}" Width="100"/>
                <DataGridTemplateColumn Header="Действие" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="▶ Перейти" Background="#27AE60" Foreground="White" 
                                    Width="70" Height="28" FontSize="11" Cursor="Hand"
                                    Click="BtnGoToProgram_Click" Tag="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>